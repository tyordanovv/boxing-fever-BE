to use the current settings fetch docker mysql container -> tyordanovv/boxing-fever-db
otherwise run a SQL server and change the application.properties (change the SQL drivers too if you wont use MYSQL)